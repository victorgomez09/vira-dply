# Opcional: Si tu kubeconfig está en una ruta no estándar. 
# Si esta línea se comenta, usará ~/.kube/config por defecto.
KUBECONFIG_PATH=/Users/tu-usuario/.kube/config-minikube

CONTAINER_ENGINE=docker  # o 'podman' según tu preferencia

# --- Configuración del Registro Local ---
# Dirección donde el controlador local accede al registro (localhost:5000)
LOCAL_REGISTRY_HOST=localhost:5000 

# Dirección interna o de servicio que Kubernetes usa para acceder al registro
K8S_REGISTRY_SERVICE_HOST=my-registry-service:5000 

# Credenciales para el registro (usadas para autenticación local y ImagePullSecret)
REGISTRY_USERNAME=registryuser
REGISTRY_PASSWORD=registrypass
REGISTRY_EMAIL=user@paas.com

# Nombre del secreto que contendrá las credenciales en Kubernetes
REGISTRY_PULL_SECRET_NAME=local-registry-secret