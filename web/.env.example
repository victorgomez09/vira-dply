# API URL - not needed in production as API is served from the same origin
# In development, Vite proxy will forward /api requests to the backend
# VITE_API_URL is optional and only used for development against a different host
